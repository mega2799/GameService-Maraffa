package infrastructure.mongo;

import domain.port.outbound.IStatisticPort;

/**
 * An abstract class with the methods of the statistic manager for a generic
 * database. Implements the domain port {@link IStatisticPort}.
 */
public abstract class AbstractStatisticManager implements IStatisticPort {
}
